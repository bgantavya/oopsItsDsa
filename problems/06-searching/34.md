Optimal beats 100% in 7+ languages

# Problem
<!-- Problem statement with number here -->
[
    34. Find First and Last Position of Element in Sorted Array
Given an array of integers nums sorted in non-decreasing order, find the starting and ending position of a given target value.
If target is not found in the array, return [-1, -1].
You must write an algorithm with O(log n) runtime complexity.
](
https://leetcode.com/problems/find-first-and-last-position-of-element-in-sorted-array/)
<!-- Link:  -->

---

# Intuition
<!-- Describe your first thoughts on how to solve this problem. -->
forget about whole problem statement and focus on `find` & `Position of element in sorted array`
so all we have to do is firstly search for the taget element through binary search

# Approach
<!-- Describe your approach to solving the problem. -->
then cleverly when num in array equals to target
lets find first and last of that type
and to do so we will iterate +- 1 in num to find initial and starting points wit element same as target

# Complexity
- Time complexity: $$O(log(n))$$
<!-- Add your time complexity here, e.g. $$O(n)$$ -->
- Space complexity: $$O(1)$$
<!-- Add your space complexity here, e.g. $$O(n)$$ -->

# Code
```C []
/**
 * Note: The returned array must be malloced, assume caller calls free().
 */
int* searchRange(int* nums, int numsSize, int target, int* returnSize) {
    int start = 0, end = numsSize - 1;
    int* ans = (int*)malloc(2 * sizeof(int));
    ans[0] = -1;
    ans[1] = -1;
    *returnSize = 2;
    while (start <= end) {
        int mid = start + (end - start) / 2;
        if (nums[mid] == target) {
            int num = mid;
            while (mid >= 0 && nums[mid] == target)
                mid--;
            ans[0] = mid + 1;
            while (num < numsSize && nums[num] == target)
                num++;
            ans[1] = num - 1;
            break;
        } else if (nums[mid] > target)
            end = mid - 1;
        else
            start = mid + 1;
    }
    return ans;
}
```
```C++ []
class Solution {
public:
    vector<int> searchRange(vector<int>& nums, int target) {
        int start = 0, end = nums.size() - 1;
        vector<int> ans(2, -1);
        while (start <= end) {
            int mid = start + (end - start) / 2;
            if (nums[mid] == target) {
                int num = mid;
                while (mid >= 0 && nums[mid] == target)
                    mid--;
                ans[0] = mid + 1;
                while (num < nums.size() && nums[num] == target)
                    num++;
                ans[1] = num - 1;
                break;
            } else if (nums[mid] > target)
                end = mid - 1;
            else
                start = mid + 1;
        }
        return ans;
    }
};
```
```C# []
public class Solution {
    public int[] SearchRange(int[] nums, int target) {
        int start = 0, end = nums.Length - 1;
        int[] ans = {-1, -1};
        while (start <= end) {
            int mid = start + (end - start) / 2;
            if (nums[mid] == target) {
                int num = mid;
                while (mid >= 0 && nums[mid] == target)
                    mid--;
                ans[0] = mid + 1;
                while (num < nums.Length && nums[num] == target)
                    num++;
                ans[1] = num - 1;
                break;
            } else if (nums[mid] > target)
                end = mid - 1;
            else
                start = mid + 1;
        }
        return ans;
    }
}
```
```Java []
class Solution {
    public int[] searchRange(int[] nums, int target) {
        int[] ans = {-1,-1};
        int start = 0;
        int end = nums.length-1;
        while(start <= end){
            int mid = start + (end-start)/2;
            if(nums[mid] == target){
                while(mid >= start  && nums[mid]==target) mid--;
                ans[0] = mid+1;
                mid++;
                while( mid <= end && nums[mid]==target) mid++;
                ans[1]=mid-1;
                break;
            }
            else if(nums[mid] > target) end = mid-1;
            else start = mid+1;
        }
        return ans;
    }
}
```
```JavaScript []
/**
 * @param {number[]} nums
 * @param {number} target
 * @return {number[]}
 */
var searchRange = function(nums, target) {
        let start = 0, end = nums.length - 1;
        let ans = [-1, -1];
        while (start <= end) {
            let mid = start + Math.floor((end - start) / 2);
            if (nums[mid] == target) {
                let num = mid;
                while (mid >= 0 && nums[mid] == target)
                    mid--;
                ans[0] = mid + 1;
                while (num < nums.length && nums[num] == target)
                    num++;
                ans[1] = num - 1;
                break;
            } else if (nums[mid] > target)
                end = mid - 1;
            else
                start = mid + 1;
        }
        return ans;
};
```
```Python []
class Solution(object):
    def searchRange(self, nums, target):
        """
        :type nums: List[int]
        :type target: int
        :rtype: List[int]
        """
        start = 0
        end = len(nums) - 1
        ans = [-1, -1]
        while (start <= end):
            mid = start + (end - start) // 2
            if (nums[mid] == target):
                num = mid
                while (mid >= 0 and nums[mid] == target):
                    mid-=1
                ans[0] = mid + 1
                while (num < len(nums) and nums[num] == target):
                    num+=1
                ans[1] = num - 1
                break
            elif (nums[mid] > target):
                end = mid - 1
            else:
                start = mid + 1
        return ans
```        
```TypeScript []
function searchRange(nums: number[], target: number): number[] {
     let start = 0, end = nums.length - 1;
        let ans = [-1, -1];
        while (start <= end) {
            let mid = start + Math.floor((end - start) / 2);
            if (nums[mid] == target) {
                let num = mid;
                while (mid >= 0 && nums[mid] == target)
                    mid--;
                ans[0] = mid + 1;
                while (num < nums.length && nums[num] == target)
                    num++;
                ans[1] = num - 1;
                break;
            } else if (nums[mid] > target)
                end = mid - 1;
            else
                start = mid + 1;
        }
        return ans;
};
```