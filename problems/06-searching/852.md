
# Problem
<!-- Problem statement with number here -->
[
    852. Peak Index in a Mountain Array
You are given an integer mountain array arr of length n where the values increase to a peak element and then decrease.
Return the index of the peak element.
Your task is to solve it in O(log(n)) time complexity.
](
    https://leetcode.com/problems/peak-index-in-a-mountain-array
)
<!-- Link:  -->

---

# Intuition
<!-- Describe your first thoughts on how to solve this problem. -->
what is peak element = element in nums which has a smaller element on both sides

# Approach
<!-- Describe your approach to solving the problem. -->
so we can do a binary search here
if nums[i] > nums[i-1]  starting point = i + 1
otherwise we will reduce ending point

# Complexity
- Time complexity: $$O(log(n))$$
<!-- Add your time complexity here, e.g. $$O(n)$$ -->
- Space complexity: $$O(1)$$
<!-- Add your space complexity here, e.g. $$O(n)$$ -->

# Code
```C []
int peakIndexInMountainArray(int* arr, int arrSize) {
        int high = arrSize-1;
        int start = 0;
        while(start < high){
            int mid = start + (high-start) /2;
            if(arr[mid] > arr[mid+1])
                high = mid;
            else
                start = mid +1;
        }
        return high;
    }
```
```C++ []
class Solution {
public:
    int peakIndexInMountainArray(vector<int>& arr) {
        int high = arr.size()-1;
        int start = 0;
        while(start < high){
            int mid = start + (high-start) /2;
            if(arr[mid] > arr[mid+1])
                high = mid;
            else
                start = mid +1;
        }
        return high;
    }
};
```
```C# []
public class Solution {
    public int PeakIndexInMountainArray(int[] arr) {
        int high = arr.Length-1;
        int start = 0;
        while(start < high){
            int mid = start + (high-start) /2;
            if(arr[mid] > arr[mid+1])
                high = mid;
            else
                start = mid +1;
        }
        return high;
    }
}
```
```Java []
class Solution {
    public int peakIndexInMountainArray(int[] arr) {
        int high = arr.length-1;
        int start = 0;
        while(start < high){
            int mid = start + (high-start) /2;
            if(arr[mid] > arr[mid+1])
                high = mid;
            else
                start = mid +1;
        }
        return high;
    }
}
```
```JavaScript []
/**
 * @param {number[]} arr
 * @return {number}
 */
var peakIndexInMountainArray = function(arr) {
    let high = arr.length-1;
        let start = 0;
        while(start < high){
            let mid = start + Math.floor((high-start) /2);
            if(arr[mid] > arr[mid+1])
                high = mid;
            else
                start = mid +1;
        }
        return high;
};
```
```Python []
high = len(arr)-1
        start = 0
        while(start < high):
            mid = start + (high-start) //2
            if(arr[mid] > arr[mid+1]):
                high = mid
            else:
                start = mid +1
        return high
```        
```TypeScript []
function peakIndexInMountainArray(arr: number[]): number {
    let high = arr.length-1;
        let start = 0;
        while(start < high){
            let mid = start + Math.floor((high-start) /2);
            if(arr[mid] > arr[mid+1])
                high = mid;
            else
                start = mid +1;
        }
        return high;
};
```