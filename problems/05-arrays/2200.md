Optimal beats 100% in 7+ languages

# Problem
<!-- Problem statement with number here -->
[
    2200. Find All K-Distant Indices in an Array]
You are given a 0-indexed integer array nums and two integers key and k. A k-distant index is an index i of nums for which there exists at least one index j such that |i - j| <= k and nums[j] == key.
Return a list of all k-distant indices sorted in increasing order.
](
https://leetcode.com/problems/find-all-k-distant-indices-in-an-array/description/)
<!-- Link:  -->

---

# Intuition
<!-- Describe your first thoughts on how to solve this problem. -->

# Approach
<!-- Describe your approach to solving the problem. -->

# Complexity
- Time complexity: $$O(n*m)$$
<!-- Add your time complexity here, e.g. $$O(n)$$ -->
- Space complexity: $$O(n)$$
<!-- Add your space complexity here, e.g. $$O(n)$$ -->

# Code
```C []
/**
 * Note: The returned array must be malloced, assume caller calls free().
 */
int* findKDistantIndices(int* nums, int numsSize, int key, int k,
                         int* returnSize) {
    int n = numsSize;
    int* index = (int*)malloc(numsSize * sizeof(int));
    int count = 0;
    for (int i = 0; i < n; i++)
        if (nums[i] == key)
            index[count++] = i;
    *returnSize = count;
    int* arr = (int*)malloc(numsSize * sizeof(int));
    // int a = index.Count;
    count = 0;
    for (int i = 0; i < n; i++) {
        for (int j = 0; j < *returnSize; j++) {
            int val = index[j] - i;
            if (val <= k && val >= -k) {
                arr[count++] = i;
                break;
            }
        }
    }
    *returnSize = count;
    free(index);
    return arr;
}
```
```C++ []
class Solution {
public:
    vector<int> findKDistantIndices(vector<int>& nums, int key, int k) {
        int n = nums.size();
        std::vector<int> index;
        for (int i = 0; i < n; i++) if (nums[i] == key) index.push_back(i);
        std::vector<int> arr;
        int a = index.size();
        for (int i = 0; i < n; i++) {
            for (int j = 0; j < a; j++) {
                int val = index[j] - i;
                if(val <= k && val >= -k) {
                    arr.push_back(i);
                    break;
                }
        }
        }
        return arr;
    }
};
```
```C# []
public class Solution {
    public IList<int> FindKDistantIndices(int[] nums, int key, int k) {
        int n = nums.Length;
        List<int> index = new List<int>();
        for (int i = 0; i < n; i++) if (nums[i] == key) index.Add(i);
        List<int> arr = new List<int>();
        int a = index.Count;
        for (int i = 0; i < n; i++) {
            for (int j = 0; j < a; j++) {
                int val = index[j] - i;
                if(val <= k && val >= -k) {
                    arr.Add(i);
                    break;
                }
        }
        }
        return arr;
    }
}
```
```Java []
class Solution {
    public List<Integer> findKDistantIndices(int[] nums, int key, int k) {
        int n = nums.length;
        List<Integer> index = new ArrayList<>();
        for (int i = 0; i < n; i++) if (nums[i] == key) index.add(i);
        List<Integer> arr = new ArrayList<>();
        int a = index.size();
        for (int i = 0; i < n; i++) {
            for (int j = 0; j < a; j++) {
                int val = index.get(j) - i;
                if(val <= k && val >= -k) {
                    arr.add(i);
                    break;
                }
        }
        }
        return arr;
    }
}
```
```JavaScript []
/**
 * @param {number[]} nums
 * @param {number} key
 * @param {number} k
 * @return {number[]}
 */
var findKDistantIndices = function(nums, key, k) {
    let n = nums.length;
        let index = [];
        for (let i = 0; i < n; i++) if (nums[i] == key) index.push(i);
        let arr = [];
        let a = index.length;
        for (let i = 0; i < n; i++) {
            for (let j = 0; j < a; j++) {
                let val = index[j] - i;
                if(val <= k && val >= -k) {
                    arr.push(i);
                    break;
                }
        }
        }
        return arr;
};
```
```Python []
class Solution(object):
    def findKDistantIndices(self, nums, key, k):
        """
        :type nums: List[int]
        :type key: int
        :type k: int
        :rtype: List[int]
        """
        n = len(nums)
        index = []
        for i in range(n): 
            if(nums[i] == key): 
                index.append(i)
        arr = []
        for i in range(n): 
            for j in index: 
                if(abs(j - i) <= k): 
                    arr.append(i)
                    break
        return arr
            
```        
```TypeScript []
function findKDistantIndices(nums: number[], key: number, k: number): number[] {
    let n = nums.length;
    let index = [];
    for (let i = 0; i < n; i++) if (nums[i] == key) index.push(i);
    let arr = [];
    for (let i = 0; i < n; i++) {
        for (const j of index) {
            if (Math.abs(j - i) <= k) {
                arr.push(i);
                break;
            }
        }
    }
    return arr;
};
```